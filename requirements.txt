# ── Core ──────────────────────────────────────────────────────────────────────
PyQt6
openai-whisper
openai

# ── Step 2 — Translation ──────────────────────────────────────────────────────
deep-translator          # Google Translate (free, no key)
google-genai             # Gemini Flash (free, needs key from aistudio.google.com)

# ── Step 4 — Voice separation ─────────────────────────────────────────────────
demucs

# ── Step 5 — TTS ──────────────────────────────────────────────────────────────
gtts
pydub
elevenlabs
audioop-lts              # Fix pydub on Python 3.13+

# ── Utils ─────────────────────────────────────────────────────────────────────
ffmpeg-python

# ── Install notes ─────────────────────────────────────────────────────────────
# ffmpeg binary (required):
#   Windows : winget install ffmpeg
#   macOS   : brew install ffmpeg
#
# Gemini API key (free):
#   → aistudio.google.com → Get API Key
#   → 15 req/min, 1500 req/day FREE
#
# OpenAI API key:
#   → platform.openai.com
#   → ~$0.002 per 3-min video