# Optional — Step ⑦ Publish thumbnail: PaddleOCR (Chinese) → Vietnamese overlay on image
#
# Install (pick instructions matching your OS / CPU / GPU):
#   https://www.paddlepaddle.org.cn/install/quick
#   https://github.com/PaddlePaddle/PaddleOCR/blob/main/readme/README_en.md
#
# Example CPU:
#   pip install paddlepaddle paddleocr numpy
#
# First run downloads OCR models (~tens of MB) under the user cache (often ~/.paddleocr/).

# Note: PaddleOCR v3+ pulls PaddleX which currently drags native deps (e.g. python-bidi)
# that may fail to build on Windows + Python 3.13 without MSVC build tools.
# Use PaddleOCR v2.x to avoid that dependency chain.
paddlepaddle
paddleocr==2.7.3
numpy
# PaddleOCR 2.7.x recommends OpenCV <= 4.6.0.66
opencv-python<=4.6.0.66
